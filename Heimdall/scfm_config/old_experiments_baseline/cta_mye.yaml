# @package _global_

###
# Set the experiment parts you would like to import
####
defaults:
  - override /model@scfm_config.model: expression_only  # the chosen model
  - override /dataset@scfm_config.dataset: mye  # dataset for the relevant task
  - override /tasks@scfm_config.tasks: cell_type_annotation_baseline  # dataset for the relevant task
  - override /scheduler@scfm_config.scheduler: cosine  # contains scheduler
  - override /trainer@scfm_config.trainer: default  # contains optimizer and trainer details
  - override /optimizer@scfm_config.optimizer: AdamW
  - override /fc@scfm_config.fc: dummy
  - override /fe@scfm_config.fe: dummy
  - override /fg@scfm_config.fg: dummy
  - override /loss@scfm_config.loss: CrossEntropyLoss

####
# Set the specific parameters you would like to change from the defaults
####
project_name: cta_mye  # project name for WandB
run_name: ${scfm_config.model.type}_lr${scfm_config.optimizer.args.lr}_bz${scfm_config.tasks.args.batchsize}  # what is set for WandB
work_dir: work_dir  # set your custom working directory
seed: 55

optimizer:
  args:
    lr: 2e-3
