type: Heimdall.fg.ESM2Fg

args:
  embedding_parameters:
    type: Heimdall.embedding.FlexibleTypeEmbedding
    constructor: from_pretrained
    args:
      embeddings: gene_embeddings
  embedding_filepath: ${data_path}/pca_embeds/${scfm_config.dataset.preprocess_args.species}_esm_embeds.pt
  d_embedding: ${scfm_config.model.args.d_model}
  frozen: true
