type: Heimdall.fg.ESM2Fg

args:
  embedding_parameters:
    type: Heimdall.embedding.FlexibleTypeEmbedding
    constructor: from_pretrained
    args:
      embeddings: gene_embeddings
  embedding_filepath: ${data_path}/pca_embeds/${scfm.dataset.preprocess_args.species}_esm_embeds.pt
  d_embedding: ${scfm.model.args.d_model}
  frozen: true
