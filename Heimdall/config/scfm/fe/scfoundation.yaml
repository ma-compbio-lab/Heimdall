type: Heimdall.fe.IdentityFe
name: Heimdall.fe.scfound

args:
  embedding_parameters:
    type: Heimdall.embedding.AutoDiscretizationEmbedding2
    args:
      dim: ${scfm.model.args.d_model}  # Replace later
      bin_num: 100
      bin_alpha: 1
      mask_token_id:
      pad_token_id:
  d_embedding: ${scfm.model.args.d_model}
  drop_zeros: true
