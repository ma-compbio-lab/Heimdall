type: Heimdall.fe.ScBERTBinningFe
name: Heimdall.fe.ScBERTBinningFe

args:
  embedding_parameters:
    type: Heimdall.embedding.FlexibleTypeEmbedding
    args:
      num_embeddings: vocab_size
      embedding_dim: ${scfm.fe.args.d_embedding}
  num_bins: 7  ## also the number of bins
  d_embedding: ${scfm.model.args.d_model}
  drop_zeros: true
